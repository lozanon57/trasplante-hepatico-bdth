export async function uploadToServer(_filePath: string, _serverIP: string) {
  return { ok: false, error: 'No disponible en demo web' };
}
export async function checkServerStatus(_serverIP: string) {
  return { online: false };
}
